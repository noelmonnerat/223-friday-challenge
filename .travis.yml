language: java

jobs:
  include:
    - stage: SonarCloud
      if: branch=develop
      script: ./gradlew sonarqube
addons:
  sonarcloud:
    organization: $SONAR_USER
    token:
      secure: $SONAR_TOKEN

jdk:
  - openjdk11
